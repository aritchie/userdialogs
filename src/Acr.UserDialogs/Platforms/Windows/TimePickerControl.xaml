<UserControl
    x:Class="Acr.UserDialogs.TimePickerControl"
    xmlns:="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Content>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TimePicker x:Name="timePicker" HorizontalAlignment="Stretch" Grid.ColumnSpan="2" Grid.Row="0" />
            <Button x:Name="btnCancel" HorizontalAlignment="Stretch" Content="Cancel" Grid.Column="0" Grid.Row="1" />
            <Button x:Name="btnOk" HorizontalAlignment="Stretch" Content="OK" Grid.Column="1" Grid.Row="1" />
        </Grid>
    </UserControl.Content>
</UserControl>
